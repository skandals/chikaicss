const tooltips = document.querySelectorAll('.tooltip');

tooltips.forEach(tooltip => {
    
    tooltip.addEventListener('pointerover', e => {
        const ccTooltip = tooltip.getAttribute('cc-tooltip');
        
    })

})